# ---- Test: logger output formatter ----
CONFIG_ZTEST=y
CONFIG_ZTEST_ASSERT_VERBOSE=1

# CAN subsystem (needed for struct can_frame definition)
CONFIG_CAN=y

CONFIG_PRINTK=y
CONFIG_UART_CONSOLE=y
CONFIG_TEST_RANDOM_GENERATOR=y
